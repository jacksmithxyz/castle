[project]
name = "castle"
version = "0.1.0"
description = "A declarative dotfiles snapshot tool"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
castle = "castle.main:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
